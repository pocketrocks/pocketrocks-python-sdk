# Vendored Bot Wire V2

This directory vendors the generated Python bot-wire package from the private
PocketRocks monorepo.

- Upstream source: `../pocketrocks/packages/shared/python/pocketrocks_bot_wire`
- Protocol version: `2`
- Sync procedure: copy the generated Python files from the upstream path, keep
  this README updated, and verify against the committed golden fixture tests in
  this repo before release.
